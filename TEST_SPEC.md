# TEST_SPEC.md — ango-atlas

<!-- scaffold template v1.12.0 から展開(2026-08-24) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提(直交性・一意性・単一帰属等)を
  **テスト内の assert で検算**し、導出過程をコメントに残す。前提を検算しない期待値は
  正しい実装を落とす(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

合成フィクスチャだけでなく、**外部データ・外部権威に対する期待値**にも同じ規律が要る。
ケースごとに、その期待値がどこから来たのかを書くこと。

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない**(超えるなら先に SPEC を上げる) |
| 外部権威(公表値・規格・検証データ) | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す。「誰かの仮定」と区別できるようにする |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。
外部データは重複や欠落を含むため、数はデータが動くたびに壊れる。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| `tests/fixtures/person1095.html` | https://www.aozora.gr.jp/index_pages/person1095.html(2026-08-24 取得) | 実データ全文。改変禁止 |
| `tests/fixtures/aoonino_excerpt.txt` / `card42877.html` | 青空文庫 card42877「青鬼の褌を洗う女」(2026-08-24 取得)。出所は `tests/fixtures/PROVENANCE.md`(往復検査のため本文にメタ情報を混ぜない) | 実データ抜粋。改変禁止 |
| 往復検査(T-101) | パーサー自身の逆写像。原文が正解であり外部正解不要 | 自己完結オラクル |
| **二重版ペア(O-1)** | 同一作品の新字新仮名版/新字旧仮名版。**ラベル無しで正解対応を持つ天然のオラクル**。ただし同題名の大半は連作(安吾巷談 12・明治開化 安吾捕物 21 等)であり、題名一致では確定できない — 本文実測で対応を取る | 実測(F-06 で確定) |
| 評価セット(O-3) | 「クエリ → 正解チャンク」30〜50 対を人手で作成。作成日と作成根拠を各対に記録 | 人手正解。作成後は凍結し、更新は専用コミット |
| 量子化損失(O-5) | 圧縮前の原次元ベクトルによる近傍順位 | 自己完結オラクル(圧縮前が正) |
| 変化点の通説区分(T-6xx) | 文学史上の通説(1931/1938–39/1946/1950–)。出典と参照日をフィクスチャ先頭に記す | 外部権威。**一致を要求するテストにはしない**(観察として記録) |

### O-1 を第一級のゲートに置く理由

二重版ペアは「同じ文体・同じ内容で、表記だけが異なる」対である。文体特徴量が表記(仮名遣い)を
拾ってしまっていれば必ずクラスタが割れる。数十件規模のラベル無し正解が自動で手に入るため、
前処理と特徴量の妥当性を毎ループ自動検証できる。**閾値は L3 の較正で実測してから確定する**
(較正前に閾値を書くと、正しい実装を落とすか、壊れた実装を通す — HC-004 / VERIF-FALSE)。


### 注記インベントリ

パーサーの分岐は `docs/notation_inventory.md`(全 513 件の実測)に基づく。
パーサーを変更したらインベントリを再生成し、往復不一致・空ベース・空本文が 0 のままかを確認する。

## ケース一覧

L1 実装済みのケースは `tests/test_aozora_index.py` / `tests/test_aozora_parser.py` に対応する。
`【validation】` は `data/raw` を前提とし、未取得環境では skip される。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-03 | ヘッダ/本文/フッタ分離 | 罫線ブロックまでがヘッダ、`底本：` 以降がフッタ。本文冒頭は実測値 |
| T-002 | F-03 | ルビの自動ベース | 本文のルビが `[('噛','かじ',False), ('米','メートル',True)]`(抽出出力)。ヘッダの用例 3 件を本文ルビに数えない |
| T-003 | F-03 | `｜` 明示ベース | base=米、explicit=True。再直列化で `｜米《メートル》` が復元される |
| T-004 | F-03 | 入力者注 `［＃…］` | verbatim 保存され、往復が一致する |
| T-005 | F-04 | 抜粋フィクスチャの往復 | `serialize(parse(x)) == x`(全文一致) |
| T-006 | F-01 | 作家ページの作品リスト抽出 | 公開中セクションの card 集合と抽出結果が**集合一致**(取りこぼし・重複なし)。全 entry に card_url / 文字遣い / 題名 |
| T-007 | F-01 | 連作の副題再構成 | `安吾巷談 01 麻薬・自殺・宗教` 等。アンカーテキストだけでは連作を区別できない |
| T-008 | F-01・F-06 | 同題名 ≠ 二重版 | 安吾巷談 12 件は全て新字新仮名(連作)。風博士は 42616/43024 が新旧の対 |
| T-009 | F-02 | カードページの作品データ | ruby zip URL・文字遣い・NDC・初出が card42877 の実測と一致。text_kind=ruby |
| T-010 | F-01 | 他作家配下のカード | `own_work=False` の集合が `{45791}`。person 001217、他著者 ヴァレリー ポール(著者) |
| T-011 | F-02 | ルビなし版フォールバック | ルビ zip が無いカードで text_kind=noruby、text_zip_url が `_txt_` zip を指す |
| T-012 | F-03 | 題名 → 副題 → 著者 | 著者はブロック最終行。card42816 は副題「（浅田一博士へ）」を著者にしない |
| T-013 | F-03 | 外字注記・二の字点がルビのベース | `顳※［＃…］`/`屡※［＃…］`/直前が仮名なら `※［＃…］` のみ。base_text は注記を落とす |
| T-101 | F-04 | 【validation】全取得テキストの往復 | 不一致 0 件(100%) |
| T-102 | F-03 | 【validation】ベースが空のルビ | 空ベースの集合が空 |
| T-103 | F-03 | 【validation】著者行 | 全文の著者行が `{坂口安吾, 坂口安吾訳}` に収まる |
| T-014 | F-02 | 作品データ表への限定 | 作品データに分類が無いカードで作家データの「分類：著者」を拾わない |
| T-201 | F-05 | 元号 → 西暦 | 明治1=1868 / 大正1=1912 / 昭和1=1926。漢数字も換算する |
| T-202 | F-05 | 初出文字列からの年抽出 | 西暦と元号の両方を返す。期間は先頭の年。「発表年月日未詳」は None |
| T-203 | F-05 | 出所の食い違い | card45737 はカード 1942 / フッタ 1982。元号(昭和17)と整合する 1942 を採り conflict を立てる |
| T-204 | F-05 | 連作の分解 | `安吾巷談 01 …` → (安吾巷談, 1)。連作でない題名は (None, None) |
| T-205 | F-05 | ジャンルの語彙と根拠 | 統制語彙内。連作は NDC より優先。NDC が無いカードは needs_review で null |
| T-206 | F-05 | 【validation】メタデータの被覆 | aozora_works.json と card_id 集合一致。必須欄を満たす |
| T-207 | F-06 | 漢字骨格 | 仮名・記号が落ち、新旧仮名で同一の骨格になる |
| T-208 | F-06 | Jaccard の境界 | 空集合で 0、同一で 1 |
| T-209 | F-06 | 【validation】閾値が空の谷にある | 一致群の最小が非一致群の最大の 10 倍超。閾値がその間にある |
| T-210 | F-06 | 【validation】題名では取りこぼす | 石の思ひ⇔石の思い 等の題名違いが検出される。連作は誤検出されない |
| T-211 | F-06 | 【validation】ペアの一意性 | 1 つのカードが 2 つ以上のペアに属さない |
| T-212 | F-07 | 折りたたみの同一化 | 新旧仮名の同内容が同一表現になる |
| T-213 | F-07 | 規則の適用順 | ハ行転呼を長音より先に適用する(しまふ = しまう) |
| T-214 | F-07 | 踊り字の展開 | ゝ ゞ 々 と、くの字点 ／＼ ／″＼ |
| T-215 | F-07・O-1 | 【validation】折りたたみの効果 | 50 組すべてで悪化しない。中央値・最小が上がる。規則群は累積で単調 |
| T-301 | F-08 | 文の分割と文長 | 手計算フィクスチャ。行頭の字下げは長さに数えない |
| T-302 | F-08 | 文字種比率 | 期待値は裸の数値でなく列挙で書く(HC-019(b)) |
| T-303 | F-08 | 句読点密度・会話文比率 | 同上 |
| T-304 | F-08 | 閉じ括弧の無い会話 | 行末までを会話とみなす |
| T-305 | F-08 | 品詞は畳んだ表現の上で取る | 新旧仮名の同内容で pos_/func_ が一致する |
| T-306 | F-08 | 【validation】分布の健全性 | 非有限値ゼロ。全件同値の退化特徴量が無い(HC-022) |
| T-307 | F-08 | 【validation】集約と全体の一致 | チャンク平均と全文一括の比率が乖離しない(HC-022) |
| T-308 | F-11 | 【validation】一覧の被覆 | works.json が全作品を覆い、必須欄と近傍を持つ |
| T-309 | F-12 | 【validation】本文の保存 | 段落化した本文がパーサーの本文と 1 字も違わない |
| T-310 | F-10 | 【validation】食い違い旗 | 近傍多数決の定義どおりに立つ |
| T-311 | N-06 | 【validation】再現性 | 同一 seed の k-means と PCA が二回実行で一致 |
| T-312 | F-10..12 | 【validation】ページの実データ実行 | node の DOM スタブで 3 ページが例外なく描画する |
| T-401 | F-13 | 段落境界の優先 | 下限に達するまで段落を束ね、上限を超える段落は分割する |
| T-402 | F-13 | 取りこぼしが無い | 全チャンクを結合すると原本文に一致する |
| T-403 | F-13 | 長い段落の分割 | 句点で切る。句点が無ければ上限で切る |
| T-404 | F-13 | 文脈の前置き | 埋め込み用の文脈は本文に混ぜず、元のチャンクを書き換えない |
| T-405 | F-13 | 【validation】全作品の被覆 | 全作品がチャンク化され、本文が保存されている |
| T-406 | F-13 | 極小チャンクの併合 | 隣へ併合し、上限超過は均等に割り直す。作品全体が短ければ 1 個のまま |
| T-407 | F-13 | 【validation】極小チャンクが残らない | 併合できるのに残っているものが無い(隣と併合すると上限超過になる場合のみ許す) |
| T-408 | F-14 | to_modern は助詞を壊さない | は/へ/を を保つ(fold との決定的な違い) |
| T-409 | F-14 | 拗音・長音・ハ行転呼 | てふてふ→ちょうちょう、幸ひ→幸い 等 |
| T-410 | F-14 | 現代文への副作用が小さい | すでに現代仮名遣いの文はほぼ変えない |
| T-411 | F-14 | 【validation】二重版一致の改善 | 中央値が上がり、悪化が改善の 1/10 未満 |
| T-412 | F-14 | 【validation】評価セットの健全性 | 正解チャンクが実在し、想定作品に属する。30 問以上 |
| T-413 | F-14 | 【validation】問いが語の一致で解けない | 問いと正解本文の 6 字以上の共通部分に漢字が 2 つ以上入らない |
| T-414 | F-14 | 【validation】二重版チャンク対応の健全性 | 一対一で、別作品を指していない |
| T-415 | F-15 | 中心化しない射影は無損失 | 全次元を残せば内積が一致する。中心化すると一致しない |
| T-416 | F-15 | int8 の往復 | 量子化しても向きが崩れない(コサイン 0.99 以上) |
| T-417 | N-04 | 【validation】配信量 | 検索データ 3 点の合計が 8 MB 以下 |
| T-418 | F-15 | 【validation】配信物と索引の整合 | 行数・次元が一致し、L2 正規化されている |
| T-419 | F-15 | 【validation】採用値の裏付け | 採用した次元・型の測定行が較正表に存在し、int8 より良い |
| T-501 | F-17 | 【validation】BM25 の二実装照合 | JS(web/search.js)と Python 参照が同じ分かち書き・同じ上位・同じ点数 |
| T-502 | F-16 | 【validation】ベクトル近傍の二実装照合 | JS と numpy が配信 float16 から同じ上位を返す |
| T-503 | F-18 | 【validation】版重複排除 | 各ペアのどちらか一方だけが除外され、新仮名の側が残る。除外作品の文書長は 0 |
| T-504 | F-16 | 【validation】多様性制限 | 同一作品からの結果が 2 件を超えない |
| T-505 | N-05 | 【validation】近傍計算の所要時間 | 1 問 300 ms 以下(実測 15.3 ms / node・15,430 本 × 176 次元) |
| T-601 | F-21 | PELT が段差を当てる | 合成系列の段差位置を返す。前提(段差 ≫ 雑音)をテスト内で検算 |
| T-602 | F-21 | 雑音では何も返さない | 純雑音で変化点ゼロ |
| T-603 | F-21・N-06 | 多変量・再現性 | 同じ入力で同じ結果 |
| T-604 | F-21 | 【validation】結果は記録するだけ | 成果物に年・特徴量・通説との重なりが残る。**一致は assert しない** |
| T-605 | F-21 | 罰則が較正されている | 理論値のままでは雑音の 8 割で誤検出。採用値で誤検出ゼロ |
| T-606 | F-19 | c-TF-IDF の性質 | 全主題に均等な語より、その主題に偏る語を上に置く |
| T-607 | F-19 | 除外語の意図 | 形式名詞は落とし、主題語(女・人間・日本)は残す |
| T-608 | F-22・N-06 | Louvain が塊を見つける | 二つの完全グラフを細い辺で結んだ図で 2 群に分かれ、再現する |
| T-609 | F-19 | 【validation】全チャンクに主題 | 件数が合い、ノイズが存在しない |
| T-610 | F-20 | 【validation】生成物の明示 | 全主題に名前があり、生成物である旨と k が記録されている |
| T-611 | F-22 | 【validation】辺は実測のみ | コミュニティの代表語が実際にその作品群の本文に出る |

### L2 以降の予定ケース

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-701 | F-24 | 固定フッタ | 全ページで本文末尾が隠れない |
