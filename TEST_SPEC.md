# TEST_SPEC.md — ango-atlas

<!-- scaffold template v1.12.0 から展開(2026-08-24) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提(直交性・一意性・単一帰属等)を
  **テスト内の assert で検算**し、導出過程をコメントに残す。前提を検算しない期待値は
  正しい実装を落とす(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

合成フィクスチャだけでなく、**外部データ・外部権威に対する期待値**にも同じ規律が要る。
ケースごとに、その期待値がどこから来たのかを書くこと。

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない**(超えるなら先に SPEC を上げる) |
| 外部権威(公表値・規格・検証データ) | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す。「誰かの仮定」と区別できるようにする |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。
外部データは重複や欠落を含むため、数はデータが動くたびに壊れる。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| `tests/fixtures/person*.html` | 青空文庫 坂口安吾 作家ページ(URL・取得日を L1 で記録) | 実データ全文。改変禁止 |
| `tests/fixtures/*_excerpt.txt` | 青空文庫 各カードのルビありテキスト抜粋(カード URL・取得日をファイル先頭に記録) | 実データ抜粋。改変禁止 |
| 往復検査(T-101) | パーサー自身の逆写像。原文が正解であり外部正解不要 | 自己完結オラクル |
| **二重版ペア(O-1)** | 同一作品の新字新仮名版/新字旧仮名版。**ラベル無しで正解対応を持つ天然のオラクル** | 実測(F-06 で確定) |
| 評価セット(O-3) | 「クエリ → 正解チャンク」30〜50 対を人手で作成。作成日と作成根拠を各対に記録 | 人手正解。作成後は凍結し、更新は専用コミット |
| 量子化損失(O-5) | 圧縮前の原次元ベクトルによる近傍順位 | 自己完結オラクル(圧縮前が正) |
| 変化点の通説区分(T-6xx) | 文学史上の通説(1931/1938–39/1946/1950–)。出典と参照日をフィクスチャ先頭に記す | 外部権威。**一致を要求するテストにはしない**(観察として記録) |

### O-1 を第一級のゲートに置く理由

二重版ペアは「同じ文体・同じ内容で、表記だけが異なる」対である。文体特徴量が表記(仮名遣い)を
拾ってしまっていれば必ずクラスタが割れる。数十件規模のラベル無し正解が自動で手に入るため、
前処理と特徴量の妥当性を毎ループ自動検証できる。**閾値は L3 の較正で実測してから確定する**
(較正前に閾値を書くと、正しい実装を落とすか、壊れた実装を通す — HC-004 / VERIF-FALSE)。

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-03 | 実抜粋のヘッダ/本文/フッタ分離 | 罫線までがヘッダ、`［＃本文終わり］` 以降がフッタ |
| T-002 | F-03 | ルビ自動ベース(漢字連続・カタカナ) | 実測した base の確定(実測日をコメントに残す) |
| T-003 | F-03 | `｜` 明示ベース | base を正しく取り、再直列化で `｜` が復元される |
| T-004 | F-03 | 入力者注 `［＃…］` | verbatim 保存 |
| T-005 | F-04 | 抜粋フィクスチャの往復 | `serialize(parse(x)) == x`(全文一致) |
| T-006 | F-01 | 作家ページの作品リスト抽出 | 公開中リストの項目と抽出結果が全単射(取りこぼし・重複なし)。各 entry にカード URL |
| T-101 | F-04 | 【validation】全取得テキストの往復 | 不一致 0 件(100%) |
| T-102 | F-02 | 再実行時のキャッシュ優先 | 2 回目実行の HTTP アクセス数 0 |
| T-201 | F-05 | メタデータの語彙・必須欄 | genre が統制語彙内、pub_year は int または null、推定値は `estimated=true` で区別 |
| T-202 | F-05 | 文字づかい種別 | 全作品が 新字新仮名 / 新字旧仮名 のいずれかに分類され、出所はカード情報の実測 |
| T-203 | F-06 | 二重版ペアの整合 | ペアの両側が別 work_id・同一正規化題名。1 作品が 2 つ以上のペアに属さない |
| T-204 | F-07 | 旧仮名 → 新仮名 正規化の可逆性境界 | 手作りの最小ケース(ゐ/ゑ/ぢ/づ/歴史的仮名遣いの語尾)で期待値を明示。**正規化前系統が失われないこと** |
| T-205 | F-07 | 正規化がルビ・注記を壊さない | 正規化後も本文とルビの対応が保たれる |
| T-301 | F-08 | 文体特徴量の抽出器を手計算フィクスチャで較正 | 短い自作テキストに対する各特徴量が手計算値と一致(導出をコメントに残す) |
| T-302 | F-08 | 層別の効果 | 同一作品を長さの異なるチャンク集合に分けても、作品レベル特徴量の分布が安定する |
| T-303 | F-09 | 再現性(N-06) | 同一入力・同一 seed の二回実行でクラスタ割当が完全一致 |
| T-304 | **O-1** | 【validation】二重版一致 | ペアの両側が同一クラスタに入る割合と ARI。**閾値は L3 較正の実測値で確定**(較正前は測定のみ) |
| T-305 | F-09 | ARI の算出 | ジャンル/時期ラベルとの ARI が算出され、シルエット係数を判定に使っていない |
| T-306 | F-10 | 不一致作品の抽出 | ジャンルラベルと文体クラスタが食い違う作品集合が決定的に列挙される |
| T-401 | F-13 | チャンク分割 | 段落境界を優先し、上限字数を超えない。作品 ID・位置が全チャンクに付く。取りこぼした本文が無い(結合すると原本文に一致) |
| T-402 | F-14 | 【validation】評価セットの Recall@10 | 2 モデルの実測値が記録され、採用モデルの根拠が `docs/` に残る |
| T-403 | **O-3** | 【validation】既知近傍 | 「堕落論」核心段落 query の上位に「続堕落論」「日本文化私観」「教祖の文学」が入る |
| T-404 | **O-5** | 圧縮損失 | 圧縮後の近傍上位 50 件と原次元の一致度(Recall@10 / Kendall τ)を実測し、採用値を SPEC に反映 |
| T-405 | N-04 | 配信サイズ | 検索用データの合計が 8 MB 以下 |
| T-501 | F-16 | 一節検索の多様性制限 | 同一作品からの結果が上限件数を超えない |
| T-502 | F-18 | 版重複排除 | 索引に載るのは二重版のうち片方のみ。検索結果に同一作品の別版が並ばない |
| T-503 | F-17 | BM25 の固有名詞検索 | 実測で本文に存在する固有名詞が、その作品を上位に返す |
| T-504 | N-05 | 近傍計算の所要時間 | 実測環境を記録した上で 300 ms 以下 |
| T-601 | F-19 | トピック分布による作品表現 | 作品ベクトルがチャンク平均で作られていない(実装検査 + 長編で個性が消えないこと) |
| T-602 | F-19 | 再現性(N-06) | UMAP/HDBSCAN の seed 固定で二回実行の割当が一致 |
| T-603 | F-21 | 変化点検出 | 検出された変化点が決定的に再現される。**通説区分との一致は assert しない**(観察として出力) |
| T-604 | F-22 | 言及グラフの辺 | 全ての辺が本文中の共起実測に裏付けられる(推定辺 0) |
| T-605 | F-20 | 生成物の区別 | 生成トピック名が本文・実測値と区別可能な形で保持されている |
| T-701 | F-24 | 固定フッタ | 全ページでフッタが存在し、body padding により本文末尾が隠れない |
| T-702 | F-23 | 相互リンク | 全ビュー間ナビとリーダーへのリンクが全ページに存在し、リンク先が実在する |
