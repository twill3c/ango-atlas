# SPEC.md — ango-atlas(安吾アトラス)

<!-- scaffold template v1.12.0 から展開(2026-08-24)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

青空文庫収録の坂口安吾コーパス(小説・評論・随筆・探偵小説・巷談を含む多ジャンル)を、
**文体・主題・時系列・言及**の四軸で俯瞰する静的 Web アプリ。クラスタや統計そのものではなく
「同クラスタ内の近傍作品」と「クラスタ境界の作品」、そして**任意の一節から他作品の一節へ渡り歩く**
経路を見せることで、読書案内として機能させる。

サーバー・API を持たない(N-01)ため、目玉機能である一節検索は**クエリ時にモデル推論を必要としない**
設計(事前計算ベクトルの内積のみ)とする。この制約が本プロジェクトの設計上の中心的判断である。

## 2. 機能要求

| ID | 要求 | 優先度 | ループ |
|---|---|---|---|
| F-01 | 青空文庫の坂口安吾作家ページ(person1095)を実測し、公開中の全作品カード(カード ID・題名・文字づかい種別・カード URL)を `data/aozora_works.json` に記録する。**作品件数を定数で書かない**(作家ページの「公開中の作品」との集合一致で確定する — HC-016)。題名は連作の副題が `<a>` の外にあるため再構成する。安吾が訳者で他作家ディレクトリに置かれるカードは `own_work=False` で区別する。実測 2026-08-24: 公開中 513 件(新字新仮名 319 / 新字旧仮名 194)、うち他作家配下 1 件。構想時の見積もり「約 370」は誤り | must | L1 |
| F-02 | 各カードの本文テキストを取得し `data/raw/{card_id}.txt` に UTF-8 で保存する(改行コードは原文どおり — F-04 の前提)。**「ルビあり」版を優先し、無いカードは「ルビなし」版へフォールバックする**(実測 2026-08-24: 513 件中 86 件はルビなし版のみ。本文は取得できるので取得不能に分類してはならない)。どちらを取ったかを `text_kind`(ruby/noruby)に記録する。全ファイルに取得元 URL・取得日・sha256 を記録する。取得間隔は 1 秒以上、再実行はローカルキャッシュ優先。本文 zip が一切無いカードのみ `external_host=true` として理由を evidence に記録する | must | L1 |
| F-03 | 青空文庫記法パーサー: ヘッダ/フッタ(底本情報)の分離、題名・副題・著者の抽出(著者は題名ブロックの**最終行** — 副題を持つ作品があるため)、ルビ(`《》`・`｜`)、入力者注(`［＃…］`)の構造化。ルビの自動ベースは**外字注記・二の字点をベースに取り込む**(実測: `顳※［＃「需＋頁」…］《こめかみ》`)。分岐の根拠は `docs/notation_inventory.md` の全件実測に置く | must | L1 |
| F-04 | 往復検査: パース → 再直列化で本文領域が原文と完全一致する(取得済み全テキスト) | must | L1 |
| F-05 | 作品メタデータ `data/works_meta.json`: 初出年・ジャンル・文字数・文字づかい・連作(系列名と番号)。**初出年は 2 出所**(カードページの初出欄・本文フッタの初出行)から取り、食い違いは**元号と整合する側**を採って evidence に理由を残す(実測 2026-08-25: 1 件が青空文庫側の西暦誤植)。ジャンルの統制語彙は 小説 / 随筆・評論 / 探偵小説 / 巷談・ルポ / 紀行・日記 / 戯曲。**「評論」と「随筆」は分けない** — 出所(NDC 分類・連作題名)がこの 2 つを区別せず、分ければ推定で埋めることになる(HC-012)。根拠を持てない作品は `genre_source=needs_review` として null のままにする | must | L2 |
| F-06 | **二重版ペア表** `data/variant_pairs.json`: 同一作品の新字新仮名版と新字旧仮名版の対応を、**本文から漢字だけを残した骨格の 4-gram Jaccard** で確定する。仮名遣いの差は骨格に出ないため、題名に依存せず照合できる。閾値は分布の谷に置き、較正値(一致群の最小・非一致群の最大)を成果物に記録する。同一表記どうしの重複作品も同じ方法で検出し `pair_type` で区別する。**題名一致では確定できない**(実測 2026-08-25: 50 組中 14 組が題名違い。逆に同題名の大半は連作) | must | L2 |
| F-07 | 前処理: **仮名遣い非依存の折りたたみ表現**(`fold`)。旧仮名→新仮名の可読変換ではなく、新旧の双方に**同じ関数を対称に**掛けて共通表現へ畳む(踊り字展開 / 小書き→大書き / ゐゑぢづ / ハ行転呼・を / 長音 au・iu・eu。ハ行転呼を長音より先に適用する)。規則は記憶ではなく**二重版ペアの差分の実測**から導出し、効果を同じペアで測って `docs/kana_fold_calibration.md` に残す。原文は `data/raw` に不変で残り、可読表示は原文を使う(F-12)。**残差は底本差であり 1.0 を要求しない** | must | L2 |
| F-08 | 文体特徴量抽出(実測 57 次元): 文長の平均・分散、句読点密度、漢字/ひらがな/カタカナ比率、品詞比率、機能語(助詞・助動詞)頻度ベクトル、会話文比率、一人称の種別、文末表現。**品詞・機能語は `kana_fold.fold` を掛けた表現の上で取る**(実測 2026-08-25: 版間 L1 0.0218 → 0.0023、作品間 L1 は 0.1099 → 0.1057 とほぼ不変。S/N 5.0 → 45.8)。機能語の語彙は記憶ではなく**コーパスの実測上位 30**。短編・長編の分散差を吸収するため 4,000 字のチャンクに切って平均を取り、**末尾の極小チャンクは直前へ併合する**(等重みだと数十字のかけらが比率を歪める)。定義は手計算フィクスチャ(T-301..305)に明文化する | must | L3 |
| F-09 | 文体クラスタリング: 標準化 → PCA(既定。UMAP を使う場合は seed 固定と再現手順を記す)→ k-means および Ward 法階層クラスタリング。ジャンルラベル・時期ラベルとの ARI を算出する | must | L3 |
| F-10 | **文体マップ**(`web/lens.html`): PCA 散布図(クラスタ / ジャンル / 時期で着色切替)。**ジャンルが近傍と食い違う作品を強調表示**する。食い違いの定義は**近傍 6 作品のうち 4 作品以上が別ジャンル**(クラスタ全体の優勢ジャンルではなく局所の多数決 — 解釈しやすく、UI 上で近傍を並べれば読者がその場で確かめられる)。読書パスは 513 葉のデンドログラムではなく**各作品の近傍リスト**で提示する(SPEC §7 の方針に沿う) | must | L3 |
| F-11 | **年表 / 作品一覧**(`web/index.html`): 初出年 × ジャンル × 文字数の一覧・フィルタ・ソート。全ビューの入口 | must | L3 |
| F-12 | **リーダー**(`web/reader.html?w={work_id}`): パース済み本文をルビ表示付きで読むビュー。段落に安定 ID を振り、F-15 の起点にする | must | L3 |
| F-13 | チャンク分割: 段落境界を優先しつつ上限 500 字(目安 300〜500 字)で分割・結合。**極小チャンクは隣へ併合し、上限超過は均等に割り直す**(素の分割では 50 字未満が 126 件・最小 1 字だった — HC-022)。結合すると原本文に一致する(取りこぼし・重複ゼロ)ことを T-402 が守る。作品 ID・段落位置を保持し、「作品名／ジャンル／初出年」は本文に混ぜず埋め込み時に前置きする(contextual chunking)。実測 2026-08-25: 15,430 チャンク(中央値 352 字) | must | L4 |
| F-14 | 埋め込み生成と**モデル選定の実測**: ruri 系と multilingual-e5 系の 2 モデルでチャンク埋め込みを作り、**2 つのオラクル**で比較して採用モデルと入力表現を決める。(A) 二重版チャンク対応(1,026 組・人手ゼロ) — 旧仮名版のチャンクから新仮名版の対応チャンクが取れるかで**表記への頑健性**を測る。(B) 人手評価セット(35 問) — チャンクを読んで内容を別の言葉で言い換えた問いで**意味検索の質**を測る。問いに正解本文の内容語がそのまま入っていないことを T-413 が守る。入力は原文と `to_modern`(可読な現代仮名遣い)の 2 通りを比べる。**モデルの大きさは実行環境の実測で決める** — 8 コア CPU では ruri-base が全件 195 分と非現実的だったため、ruri-v3-30m(256 次元・10 分)と multilingual-e5-small(384 次元・21 分)を比較対象とする。選定根拠を `docs/embed_eval.md` に記録する | must | L4 |
| F-15 | ベクトル圧縮: 打ち切り SVD による次元削減 + 量子化。**平均を引いてはならない**(中心化すると単位長ベクトルの内積が保たれず、全次元を残しても R@10 が 0.885 止まりになる — 実測)。圧縮後の近傍上位 50 件が原次元とどれだけ一致するか(Recall@10 / Kendall τ)を実測して決める。**採用値 2026-08-25: 192 次元 × float16 = 5.93 MB、R@10 0.9925 / τ 0.983**。int8 は同次元 2.96 MB だが R@10 0.958 に落ち、192 と 256 で同値になる(次元より量子化が効く)ため採らない。配信物は `web/data/chunk_vectors.f16`(生バイト列)・`chunk_index.json`・`chunk_projection.f32` の 3 点で計 6.70 MB | must | L4 |
| F-16 | **一節から探す**(`web/search.html` + リーダー連携): リーダーの段落末の印、または検索ページの選択からチャンクを起点に、他作品の近傍一節を距離順に提示する。**同一作品からの結果は最大 2 件**に制限して多様性を確保する。クエリ時のモデル推論は行わない — 事前計算した float16 ベクトルの内積のみ。ベクトルは使うときだけ取りに行く(遅延読み込み) | must | L5 |
| F-17 | 自由語検索: BM25 転置索引を静的配信し、語句・固有名詞で検索する。索引語は**名詞の表層形**(2 文字以上、または 1 文字の漢字。数詞は除く)に限る — 目的は埋め込みが弱い固有名詞・具体名を補うことで、活用語は問い側の活用形と噛み合わないため入れない。問い側の分かち書きは**配信した語彙への最長一致**でブラウザが行う(形態素解析器を持ち込まない)。実装は JS と Python の**二実装照合**で守る(T-501) | must | L5 |
| F-18 | 検索結果の版重複排除: 二重版のある作品は**新字新仮名の側**を残して索引に載せる。同一表記の重複作品は本文の長い側を残す。除外は 52 作品(実測 2026-08-25)。**リーダーでは両版とも読める** — 除外するのは検索結果だけで、検証系(F-06)も両版を保つ | must | L5 |
| F-19 | **主題マップ**(`web/topic.html`): チャンク単位の主題抽出。**BERTopic(UMAP → HDBSCAN)は採らない** — 実測 2026-08-25 でこのコーパスの埋め込み空間に密度の谷が無く、PCA 32 次元 × min_cluster_size 15〜60 のいずれもノイズ 71〜85%、低次元では巨大クラスタ 2 個に潰れた。単一作家の散文は連続体である。代わりに **PCA → k-means(k=20、seed 固定)→ c-TF-IDF**で全チャンクに主題を割り当てる。形式名詞は**明示の除外語**で落とす(df の機械的な足切りでは「女」「人間」「日本」まで落ちるため)。作品は**チャンク平均ではなく主題分布**で表現する。主題 × 初出年(推移)と 主題 × ジャンル(同一主題の小説版・評論版)を提供し、**1 作品に偏った主題は「作品固有」と明示**する | should | L6 |
| F-20 | 主題名の生成: 各主題の代表チャンク 3 件(重心に最も近いもの)と c-TF-IDF 上位語を読んで名称と一文説明を付ける。`data/topic_names.json` に生成物である旨・生成者・k を記録し、**UI では実測の代表語と明確に区別して表示**する。k や seed を変えたら付け直す(T-610 が k の一致を守る) | should | L6 |
| F-21 | **転換点**(同ページ): 初出年ごとの文体特徴量の平均を主成分に落とし、PELT で変化点を推定する。**罰則の強さは合成データで較正する** — 理論値(BIC)のままだと純雑音の 79% で変化点を「発見」してしまう(実測)。採用値は誤検出 0% / 3σ の段差で検出力 100%。通説の時期区分を帯として重ね、**一致は主張ではなく観察として示す**(合致を要求するテストを書かない — T-604) | should | L6 |
| F-22 | **言及グラフ**(同ページ): 固有名詞 TF-IDF のコサインで作品間に辺を張り、Louvain でコミュニティを取る。辺は本文実測の共起のみ(推定辺なし)。513 ノードの網は描かず、**「コミュニティ × その特徴的な固有名詞 × 所属作品」**の形で示す。代表語は全体で普遍的な語ではなく、内側の出現率が突出する語を選ぶ。走査順を node 番号順に固定して再現する(N-06) | could | L6 |
| F-23 | 相互リンク統合(全ビュー間ナビ + 各ビューからリーダーへ)・Vercel デプロイ(本番 ango-atlas.vercel.app)・app-menu 📚 文学・翻訳(cat5)掲載・GitHub twill3c/ango-atlas 公開 | must | L7 |
| F-24 | kiko-atlas 準拠の下部固定フッタ(全ページ): © / GitHub / 歩き方 / 設計図 / App Menu。fixed 配置 + body padding-bottom で本文が隠れないこと | must | L7 |
| F-25 | (任意)自由語の意味検索: transformers.js による埋め込みモデルの**遅延ロード・オプトイン**(既定 OFF、初回ロード量を UI に明示)。L7 までの機能が成立していることを前提とし、不成立なら出荷しない | could | L7 |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的サイトとして動作(サーバー・API 不要)。データはビルド時生成の JSON / バイナリ。F-25 を除きクエリ時のモデル推論を行わない | 成果物の構成確認。ネットワークタブに外部 API 呼び出しが無いこと |
| N-02 | 青空文庫への負荷配慮: 取得間隔 1 秒以上、User-Agent 明示、再実行はキャッシュ優先で再取得しない | fetch スクリプトのコード確認 + 再実行時の HTTP アクセス数 0 |
| N-03 | 全データに出所(URL)と取得日を記録する(実測主義) | データファイルの provenance フィールド検査 |
| N-04 | 検索に要する配信データが **8 MB 以下**。本文は作品単位シャードで遅延取得する。実測 2026-08-25: ベクトル 5.43 + チャンク索引 0.23 + BM25 1.83 = **7.49 MB**(余裕 0.51)。索引は並行配列にしてある(1 チャンク 1 オブジェクトの JSON では 0.86 MB を占め予算を割った) | ビルド成果物のサイズ計測(T-417) |
| N-05 | 一節検索の応答: 1 万件規模のチャンクに対する近傍計算が**ブラウザで 300 ms 以下**(実測環境を記録する) | 計測スクリプト + 手動確認 |
| N-06 | 解析の再現性: 乱数を使う手順(k-means 初期値・UMAP・HDBSCAN)は seed を固定し、再実行で同一出力を得る。クラスタリングは scipy/sklearn に依存せず numpy だけで実装する | 同一入力での二回実行の出力一致(T-311) |

## 4. 品質基準

- **往復検査(F-04)**: 本文領域の一致率 100%。1 件でも不一致があれば L1 は完了しない(L1 実績 2026-08-24: 513/513 一致)
- **カード一覧(F-01)**: 作家ページの「公開中の作品」との**集合一致**(取りこぼし・重複ゼロ)。定数件数では書かない(HC-016)
- **二重版一致(O-1 / F-06)**: 同一作品の新字新仮名版と新字旧仮名版が同じ文体クラスタに入る。**閾値は L3 の較正で実測してから確定する**(先に数値を書かない)。割れた場合は「特徴量が文体ではなく表記を見ている」証拠として前処理(F-07)をやり直す。L2 実績 2026-08-25: 二重版 50 組・重複 2 組を確定。折りたたみ後の本文一致率は中央値 0.9964(素 0.9488)
- **既知近傍(O-3 / F-14)**: 「堕落論」の核心段落を query としたとき、「続堕落論」「日本文化私観」「教祖の文学」が上位に入る。評価セット 30〜50 問の Recall@10 をモデル選定の根拠とする
- **量子化損失(O-5 / F-15)**: 圧縮ベクトルの近傍上位 50 件と原次元の順位一致度。**採用 176 次元 float16 で R@10 0.9815 / τ 0.963、配信 5.43 MB**。192 次元(R@10 0.9925)の方が良いが BM25 索引と合わせると N-04 まで 0.02 MB しか残らず、次の小さな変更で必ず割るため 0.5 MB の余裕を買った
- **埋め込みの L4 実績(F-14、2026-08-25)**: 採用は **ruri-v3-30m・原文入力・文脈前置きあり**。オラクル A(二重版チャンク 1,026 組)は **R@1 1.000** で、原文のまま仮名遣いの違いを完全に吸収した。オラクル B(人手 35 問)は R@1 0.400 / R@10 0.571。e5-small は A 0.9981 / B R@1 0.143 と B で大きく劣る。`to_modern` による現代仮名化は A で差が無く B ではむしろ下がった(R@1 0.343)ため**埋め込みには使わない**。文脈前置きの有無は同一部分集合でほぼ互角(R@1 0.457 対 0.400)で、B の低さの原因ではない — 30M モデルの限界であり、L5 の BM25 併用(F-17)で補う
- **クラスタ妥当性(F-09)**: シルエット係数ではなく、ジャンル/時期ラベルとの **ARI** で解釈する。L3 実績 2026-08-25: ARI(ジャンル)は k=4 で最大 0.248、k=8 で 0.091。ARI(時期)は 0.002–0.110。**低いこと自体は欠陥ではない** — ジャンルと文体の食い違いを見せるのが F-10 の目的で、完全一致したら見るものが無い
- **L6 の実測(2026-08-25)**: 主題 20(全 15,430 チャンクに割当、ノイズ無し)。文体の変化点は 1931・1934・1937・1938・1944・1946 の 6 件で、うち 4 件が通説区分の近傍にあった(1950 の巷談期は検出されず)。言及コミュニティ 59 群のうち大きいものは 牧野信一圏(22 作)・芥川/葛巻圏(20 作)・囲碁将棋圏(18 作)・戦国圏(17 作)・新潟圏(12 作)で、構想が予想した交友・関心圏が実測で確認された
- **O-1 の L3 実績(2026-08-25)**: 二重版 50 組**すべて**で相手が最近傍(最悪順位 1)、k=3..10 のすべてで同一クラスタ。ただし**畳むことが O-1 を救っているわけではない** — 品詞を原文で取った対照条件でも同じ結果になり、文字レベルの特徴が支配的である。畳む効果は品詞ブロックに限られるので、チャンク単位の埋め込みを扱う L4/L5 で測り直す

## 5. スコープ外

- 坂口安吾以外の作家への拡張、英訳・対訳表示
- 本文の校正・改変(取得したままを正とする。正規化は解析用の派生系統としてのみ持つ)
- 学術的な文体論・作家論の**主張**(出すのは実測値と可視化まで。解釈は案内文の範囲に留める)
- cross-encoder リランカーの出荷(オフライン評価で精度差を記録するのみ)
- ログイン・ユーザーデータ保存・サーバーサイド機能
- 挿絵画像の収載
